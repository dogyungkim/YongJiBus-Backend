spring:
  config:
    activate:
      on-profile: local
  # h2:
  #   console:
  #     enabled: true
  #     path: /h2-console
  # datasource:
  #   url: jdbc:h2:mem:localdb
  #   username: sa
  #   password:
  #   driver-class-name: org.h2.Driver
  datasource:
    url: jdbc:mysql://127.0.0.1:6033/yongji
    username: admin
    password: dacwad-gazXy5-kupdib
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  mail:
    host: 127.0.0.1
    port: 1025
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false
            required: false
          connection-timeout: 5000
          timeout: 5000
          write-timeout: 5000
    auth-code-expiration-minutes: 10
    report:
      to: local-report@yongjibus.dev
      
secrets:
  open_api_key: ${OPEN_API_KEY}

management:  
  endpoints:  
    prometheus:  
      enabled: true  
    web:  
      exposure:  
        include: prometheus, loggers

jwt:
  secret: ${JWT_SECRET}
  access-token-validity: 36000000    # 10시간 (10 * 60 * 60 * 1000 밀리초)
  refresh-token-validity: 604800000  # 7일 (7 * 24 * 60 * 60 * 1000 밀리초)

gmail:
  enabled: false
